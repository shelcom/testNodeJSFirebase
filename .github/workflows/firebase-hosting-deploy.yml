name: Deploy to Firebase Hosting on push

on:
  push:
    branches:
      - main # This runs on every push to the "main" branch. Modify the branch name if needed.

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "16" # Use the version that matches your project (e.g., 16 or later)

      - name: Install dependencies
        run: npm install

      - name: Build the project (if applicable)
        run: npm run build # Optional, if your project requires a build step (e.g., compiling TypeScript or bundling).


    #   - name: Deploy to Firebase Hosting
    #     run: firebase deploy --only hosting
    #     env:
    #       FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }} # Use the Firebase token from GitHub secrets
